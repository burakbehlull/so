import { StyleSheet } from "react-native";

export const mainStyle = StyleSheet.create({
    container: {
        backgroundColor: 'blue',
    }
})
export const cont = StyleSheet.create({
    container: {
        backgroundColor: 'red',
    }
})






